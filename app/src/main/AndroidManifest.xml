<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wtxy.familyeducation">
    <!-- 访问网络，网络定位需要上网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" /> <!-- 用于读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" /> <!-- 写入扩展存储，向扩展卡写入数据，用于写入离线定位数据 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.StudentInfoActivity"></activity>
        <activity android:name=".activity.ClassTableActivity"></activity>
        <activity android:name=".home.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ClassInfoActivity" />
        <activity android:name=".activity.SubjectListActivity" />
        <activity android:name=".activity.TeacherInfoActivity" />
        <activity android:name=".activity.StudentHomeworkListActivity" />
        <activity android:name=".activity.StudentScoreListActivity" />
        <activity android:name=".activity.ManagerListManageActivity" />
        <activity android:name=".activity.TeacherManageListActivity" />
        <activity android:name=".home.PublishActivity" />
        <activity
            android:name=".home.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".BaseActivity" />
        <activity android:name=".login.RegistActivity" />
        <activity android:name=".login.LoginActivity" />
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".activity.ExamInfoActivity" />
        <activity android:name=".activity.HomeworkInfoActivity" />
        <activity android:name=".activity.ChoiceClassActivity" />
        <activity android:name=".activity.StudentHomeworkDetailActivity" />
        <activity
            android:name=".web.WebActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>